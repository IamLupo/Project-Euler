#include <string>
#include <vector>
#include <sstream>
#include <utility>

namespace IamLupo {
	namespace String {
		bool isPalindrome(const std::string &s);
		
		std::vector<std::string> explode(const std::string &s, char delim);
	}
}